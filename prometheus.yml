global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'gamulpung-server'
    static_configs:
      # devcontainer 내부에서 실행되는 앱 (host.docker.internal로 접근)
      - targets: ['host.docker.internal:8000']
    metrics_path: '/metrics'
